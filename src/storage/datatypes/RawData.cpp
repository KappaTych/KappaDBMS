#include "RawData.hpp"

namespace se {

// RawData& RawData::Fill(std::string value)
// {
//   std::copy(value.c_str(), value.c_str() + value.length(), data_ + len_);
//   len_ += value.length();
//   data[len_++] = 0;
//   return *this;
// }

} // namespace se